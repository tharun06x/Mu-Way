__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.env
venv/
.venv/
data/raw/*
!data/raw/.gitkeep

# Large batch output files (not needed at runtime, regenerated by pipeline)
output/feature_store.pkl
output/career_gaps.csv
output/roadmaps.json
output/health_report.json

# Temp files
~$*

# Python version file (local tooling only)
.python-version
uv.lock

# Misc
streamlit.log
roadmap_tharuntomy*.json
Domain_Mixup_Bug_Report.docx

# Note: query.xlsx, output/ranking_model.pkl, output/model_metadata.json
# are intentionally NOT ignored so they are tracked via Git LFS for HF Spaces.
